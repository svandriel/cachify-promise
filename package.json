{
  "name": "cachify-promise",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "test": "jest",
    "build": "tsc",
    "verify": "npm run test && npm run clean && npm run build",
    "watch": "tsc -w",
    "prepublish": "npm run verify"
  },
  "keywords": [],
  "author": "Sander van Driel",
  "license": "ISC",
  "engines": {
    "node": ">=10"
  },
  "devDependencies": {
    "@types/jest": "^25.1.3",
    "husky": "^4.2.3",
    "jest": "^25.1.0",
    "lint-staged": "^10.0.8",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^25.2.1",
    "tslint": "^6.0.0",
    "typescript": "^3.8.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "npm run verify"
    }
  },
  "lint-staged": {
    "packages/**/*.ts": [
      "prettier --write",
      "tslint -p . -t stylish --fix"
    ],
    "**/*.json": [
      "prettier --write"
    ]
  }
}
